<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>eWorkbench</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/CSS/reset.css">
    <link rel="stylesheet" href="/CSS/variables.css">
    <link rel="stylesheet" href="/CSS/style.css">
    

</head>
<body>

    <%#     
        The page is divided into 4 grid squares. 
        Top left corner (Logo)
        Top right section (Nav Bar)
        Bottom left menu (left Bar)
        Bottom right section (main content area)
    %>

    <div class="content-area">

        <%- include("viewModules/topBarMenus/topNavBar.ejs") %> <%# This loads the top nav bar and includes the corner and top nav items. %>

        <div id="left-section">
            <label id="page-title-label"> <%= menuTitle %></label>
            <% if (locals.leftBar_menu) { %>
                <%- include(`viewModules/leftBarMenus/${leftBar_menu}`); %>
            <% } %>
        </div>
        <div id="main-content-area">
            <% if (locals.mainOne) { %>
                <%- include(`viewModules/cards/${mainOne}`); %>
            <% } %>
            <% if (locals.mainTwo) { %>
                <%- include(`viewModules/cards/${mainTwo}`); %>
            <% } %>
            <% if (locals.mainThree) { %>
                <%- include(`viewModules/cards/${mainThree}`); %>
            <% } %>
        </div>
    </div>

</body>
</html>